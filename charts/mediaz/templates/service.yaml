apiVersion: v1
kind: Service
metadata:
  name: {{ include "mediaz.fullname" . }}
  labels:
    {{- include "mediaz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mediaz.selectorLabels" . | nindent 4 }}
